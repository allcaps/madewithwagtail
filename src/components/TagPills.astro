---
import { withBase } from '../helpers.ts';

interface Props {
  values: { name: string; slug: string; count: number }[];
  activeValue?: string | null;
}

const { values, activeValue } = Astro.props;
---

<div class="btn-group">
  {values.map((value) => (
    <a
      href={activeValue === value.slug ? withBase('/') : withBase(`/sites/tag/${value.slug}/`)}
      class={`btn -tag${activeValue === value.slug ? ' is-active' : ''}`}
    >
      {value.name}{" "}
      <span>{value.count}</span>
    </a>
  ))}
</div>
